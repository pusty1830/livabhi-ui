// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        fontFamily: 'Custom-Bold,Custom-Regular,Rosttel, Arial, sans-serif', // Include your custom font
    },
});

export default theme;
